export const environment = {
    production: false,

    // This setting can be updated at runtime using the API_URL environment variable.
    // Should include the scheme, host, port, and any API versioning prefix.
    // Use 'assets/sample-data' to point at local data objects instead of a real API.
    // apiUrl: window["env"]["apiUrl"] || "http://localhost:8000"
    apiUrl: 'assets/sample-data'
};
